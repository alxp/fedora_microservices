#!/bin/sh
#
#
#
#   Startup/shutdown script for running islandora microservices.
#
#   Linux chkconfig stuff:
#
#   chkconfig: 2345 70 10
#   description: Startup/shutdown script for running Islandora Microservices.
#
# Source function library.

PROG="Islandora Microservices"
PYTHON=python26
MICROSERVICES_PATH=/usr/local/islandora_microservices/
CONFIG_FILE=/usr/local/islandora_microservices/islandora_listener.cfg

# *** Please make sure you change the fedora user to reflect the local setup. ***

FEDORA_USER=fedora

start () {
	echo -n "Starting $PROG: "
	# check to see if the process is already running
	if [ -f /var/lock/islandora_microservices ]; then
		echo "$PROG is already running"
	else
			# start JMS as the fedora user
			#sudo -u $FEDORA_USER -E "$PYTHON ${MICROSERVICES_PATH}islandora_listener.py -C $CONFIG_FILE &" >/dev/null 2>&1
			#sudo -u $FEDORA_USER bash -c 'env PATH=$PATH:/usr/local/bin HOME=/home/fedora $PYTHON ${MICROSERVICES_PATH}islandora_listener.py -C $CONFIG_FILE &> /dev/null 2>&1'
			sudo -u $FEDORA_USER bash -c "env PATH=$PATH:/usr/local/bin HOME=/home/fedora $PYTHON ${MICROSERVICES_PATH}islandora_listener.py -C $CONFIG_FILE &> /dev/null &"
			sleep 3
			ps -ef | grep 'islandora_listener' | grep -v 'grep' >/dev/null 2>&1
			RETVAL=$?
			if [ $RETVAL = 0 ]; then
				touch /var/lock/islandora_microservices
				echo "                                          [ OK ]"
			else
				echo "                                          [ FAILED ]"
			fi
		fi
}

stop () {
	# stop daemon
	echo -n "Stopping $PROG: "
	# check to see if the process is running
	if [ ! -f /var/lock/islandora_microservices ]; then
		echo "$PROG is not running"
	else
		# use kill -2 to stop JMS.
		kill `ps -ef | grep islandora_listener | grep -v grep | awk '{print $2}'` &>/dev/null
		RETVAL=$?
		echo "                                          [ OK ]"
		[ $RETVAL = 0 ] && rm -f /var/lock/islandora_microservices
	fi
}

restart() {
	stop
	start
}

case $1 in
	start)
		start
	;;
	stop)
		stop
	;;
	restart)
		restart
	;;
	status)
		if [ -f /var/lock/islandora_microservices ]; then
			echo "$PROG (pid `ps -ef | grep 'fedora_listener' | grep -v 'grep' | awk '{print $2}'`) is running..."
		else
			echo "$PROG has not been started."
		fi
	;;
	*)

	echo $"Usage: $PROG {start|stop|restart|status}"
	exit 3
esac

exit $RETVAL
